#!/bin/bash
# Unifideck backend entrypoint
# Backend modules are now in py_modules/backend/ which gets packaged automatically
# This script is kept for potential future backend build steps

echo "Backend entrypoint executed"
exit 0
